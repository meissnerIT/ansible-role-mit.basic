# vim: ts=2 sw=2 et
---
# Size of tmpfs volume mounted to /tmp. Value will be used
# during a rewrite of /etc/fstab.
mit_basic_tmp_size: 512M

# Harden SSH config? See issue #21540.
# As of 2024-08-12, this has only been tested with Debian 11 hosts!
mit_basic_harden_ssh_debian11: false
